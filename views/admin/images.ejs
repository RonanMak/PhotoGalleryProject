<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/image.css">
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>

            <main>
                <% if (imgs.length > 0) { %>
                    <div class="grid">
                        <% for (let image of imgs) { %>
                            <article class="card image-item">
                                <header class="card__header">
                                    <h1 class="image__title">
                                        <%= image.title %>
                                    </h1>
                                </header>
                                <div class="card__image">
                                    <img src="<%= image.imageUrl %>" alt="<%= image.title %>">
                                </div>
                                <div class="card__content">
                                    <h2 class="image__date">
                                        <%= image.date %>
                                    </h2>
                                    <hr>
                                    <p class="image__description">
                                        <%= image.description %>
                                    </p>
                                </div>
                                <div class="card__actions">
                                    <a href="/admin/edit-image/<%= image.id %>?edit=true" class="btn">Edit</a>
                                    <form action="/admin/delete-image" method="POST">
                                        <input type="hidden" value="<%= image.id %>" name="imageId">
                                        <button class="btn" type="submit">Delete</button>
                                    </form>

                                </div>
                            </article>
                            <% } %>
                    </div>
                    <% } else { %>
                        <h1>No Images Found!</h1>
                        <% } %>
            </main>
            <%- include('../includes/end.ejs') %>